# Plane MCP Server Configuration
# Copy this file to .env and fill in your values

# Required: Your Plane API token
# Get this from Workspace Settings > API Tokens in your Plane workspace
PLANE_API_KEY=your-plane-api-key-here

# Required: Your workspace slug
# Found in your Plane workspace URL (e.g., if URL is https://app.plane.so/my-workspace/, then slug is "my-workspace")
PLANE_WORKSPACE_SLUG=your-workspace-slug

# Optional: Plane API host URL (defaults to https://api.plane.so/)
# Only change this if you're using a self-hosted Plane instance
PLANE_API_HOST_URL=https://api.plane.so/

# Optional: Port for the SSE server (defaults to 3000)
PORT=3000